[defaults]
remote_user = eslam
#private_key_file = ~/terraform.pem
host_key_checking = false
inventory = inventory

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False

